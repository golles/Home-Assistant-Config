type: 'custom:auto-entities'
card:
  type: grid
card_param: cards
filter:
  include:
    - domain: light
      state: 'on'
      attributes:
        is_hue_group: true
      options:
        type: light
    - entity_id: "light.wled*"
      state: 'on'
      options:
        type: light
sort:
  method: name
